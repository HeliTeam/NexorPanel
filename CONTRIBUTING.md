## Local Development Setup

- Clone the **Nexor** (`github.com/nexor/panel`) repository
- Rename `.env.example` to `.env` if present
- Run `main.go` or `go run .` with `XUI_DEBUG=true` for local assets
